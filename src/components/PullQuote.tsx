import React from 'react';

interface PullQuoteProps {
  children: React.ReactNode;
  color?: string;
}

export const PullQuote: React.FC<PullQuoteProps> = ({ children, color = '#C96E58' }) => {
  return (
    null
  );
};
